import 'package:flutter/material.dart';

class PreferredAppBarSize extends StatelessWidget {
  const PreferredAppBarSize(
      {super.key, required this.borderRadius, required this.height});

  final double borderRadius;
  final double height;

  @override
  Widget build(BuildContext context) {
    return 
  }
}
